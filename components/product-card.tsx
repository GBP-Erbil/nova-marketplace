'use client';
import Link from 'next/link'; import { Plus } from 'lucide-react'; import { money, Product } from '@/lib/products'; import { useShop } from './shop-provider';
export function ProductCard({p}:{p:Product}){const {add}=useShop();return <article className="group"><Link href={`/product/${p.id}`} className="relative block aspect-[4/5] overflow-hidden rounded-[1.4rem] bg-[#ddd8ce]"><img src={p.image} alt={p.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-[1.03]"/>{p.badge&&<span className="absolute left-4 top-4 rounded-full bg-ink px-3 py-1.5 text-[10px] font-bold uppercase tracking-wider text-cream">{p.badge}</span>}</Link><div className="flex items-start justify-between pt-4"><Link href={`/product/${p.id}`}><h3 className="font-bold">{p.name}</h3><p className="mt-1 text-sm text-black/45">{p.category}</p></Link><div className="flex items-center gap-3"><span className="font-bold">{money(p.price)}</span><button onClick={()=>add(p.id)} aria-label={`Add ${p.name} to cart`} className="grid h-9 w-9 place-items-center rounded-full bg-ink text-cream transition hover:bg-lime hover:text-ink"><Plus size={17}/></button></div></div></article>}
