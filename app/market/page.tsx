'use client';
import { useMemo,useState } from 'react'; import { Search, SlidersHorizontal } from 'lucide-react'; import { SiteHeader } from '@/components/site-header'; import { ProductCard } from '@/components/product-card'; import { products } from '@/lib/products';
export default function Page(){const [q,setQ]=useState('');const [cat,setCat]=useState('All');const [sort,setSort]=useState('featured');const cats=['All',...Array.from(new Set(products.map(p=>p.category)))];const shown=useMemo(()=>{let x=products.filter(p=>(cat==='All'||p.category===cat)&&p.name.toLowerCase().includes(q.toLowerCase()));if(sort==='low')x=[...x].sort((a,b)=>a.price-b.price);if(sort==='high')x=[...x].sort((a,b)=>b.price-a.price);return x},[q,cat,sort]);return <main className="min-h-screen bg-obsidian text-ivory"><SiteHeader/><section className="mx-auto max-w-[1500px] border-x border-gold/10 px-5 py-14 md:px-10 md:py-20"><div className="flex flex-wrap items-end justify-between gap-6"><div><p className="text-[9px] font-bold uppercase tracking-[.28em] text-gold">The private collection</p><h1 className="mt-3 font-serif text-6xl tracking-[-.05em] md:text-9xl">Acquire distinction.</h1></div><p className="max-w-sm text-sm leading-6 text-ivory/40">Four statement editions. Designed to carry presence from across the room.</p></div><div className="mt-12 flex flex-col gap-4 border-y border-gold/15 py-5 lg:flex-row lg:items-center"><label className="flex h-12 min-w-72 flex-1 items-center gap-3 border border-gold/20 bg-ivory/[.03] px-5"><Search size={16} className="text-gold"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search the collection" className="w-full bg-transparent text-sm outline-none placeholder:text-ivory/25"/></label><div className="flex gap-2 overflow-x-auto pb-1 lg:pb-0">{cats.map(c=><button key={c} onClick={()=>setCat(c)} className={`whitespace-nowrap px-4 py-2.5 text-[9px] font-bold uppercase tracking-[.15em] ${cat===c?'bg-gold text-obsidian':'border border-gold/20 text-ivory/50'}`}>{c}</button>)}</div><label className="flex items-center gap-2 text-[9px] font-bold uppercase tracking-[.16em] text-gold"><SlidersHorizontal size={14}/><select value={sort} onChange={e=>setSort(e.target.value)} className="bg-obsidian outline-none"><option value="featured">Curated</option><option value="low">Price: low to high</option><option value="high">Price: high to low</option></select></label></div><div className="mb-7 mt-8 text-[9px] uppercase tracking-[.2em] text-ivory/35">{shown.length} collector pieces</div>{shown.length?<div className="grid gap-x-6 gap-y-14 sm:grid-cols-2 lg:grid-cols-4">{shown.map(p=><ProductCard key={p.id} p={p}/>)}</div>:<div className="border border-dashed border-gold/25 py-24 text-center"><h2 className="font-serif text-3xl">No piece found</h2><p className="mt-2 text-sm text-ivory/35">Try another search or collection.</p></div>}</section></main>}
