'use client';
import { useMemo,useState } from 'react'; import { Search, SlidersHorizontal } from 'lucide-react'; import { SiteHeader } from '@/components/site-header'; import { ProductCard } from '@/components/product-card'; import { products } from '@/lib/products';
export default function Page(){const [q,setQ]=useState('');const [cat,setCat]=useState('All');const [sort,setSort]=useState('featured');const cats=['All',...Array.from(new Set(products.map(p=>p.category)))];const shown=useMemo(()=>{let x=products.filter(p=>(cat==='All'||p.category===cat)&&p.name.toLowerCase().includes(q.toLowerCase()));if(sort==='low')x=[...x].sort((a,b)=>a.price-b.price);if(sort==='high')x=[...x].sort((a,b)=>b.price-a.price);return x},[q,cat,sort]);return <main className="min-h-screen bg-cream text-ink"><SiteHeader/><section className="mx-auto max-w-[1440px] px-5 py-12 md:px-10 md:py-16"><p className="text-xs font-bold uppercase tracking-[.2em] text-black/40">The complete collection</p><h1 className="mt-3 text-6xl font-black tracking-[-.06em] md:text-8xl">Market</h1><div className="mt-10 flex flex-col gap-4 border-y border-black/10 py-5 lg:flex-row lg:items-center"><label className="flex h-12 min-w-72 flex-1 items-center gap-3 rounded-full bg-white px-5"><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search all objects" className="w-full bg-transparent text-sm outline-none"/></label><div className="flex gap-2 overflow-x-auto pb-1 lg:pb-0">{cats.map(c=><button key={c} onClick={()=>setCat(c)} className={`whitespace-nowrap rounded-full px-4 py-2.5 text-sm font-bold ${cat===c?'bg-ink text-cream':'border border-black/15'}`}>{c}</button>)}</div><label className="flex items-center gap-2 text-sm font-bold"><SlidersHorizontal size={16}/><select value={sort} onChange={e=>setSort(e.target.value)} className="bg-transparent outline-none"><option value="featured">Featured</option><option value="low">Price: low to high</option><option value="high">Price: high to low</option></select></label></div><div className="mb-6 mt-8 text-sm text-black/45">{shown.length} objects</div>{shown.length?<div className="grid gap-x-5 gap-y-12 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{shown.map(p=><ProductCard key={p.id} p={p}/>)}</div>:<div className="rounded-3xl border border-dashed border-black/20 py-24 text-center"><h2 className="text-2xl font-bold">No objects found</h2><p className="mt-2 text-black/45">Try another search or category.</p></div>}</section></main>}
